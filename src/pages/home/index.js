import React from 'react';
import { View, ScrollView, SafeAreaView } from 'react-native'

function Home(props) {
    return (
        <SafeAreaView>

        </SafeAreaView>
    );
}

export default Home;